export const BREADCRUMB_DEMO_CODE = `import { Breadcrumb } from '../../blocks';

export const BreadcrumbDemo = () => {
	return <Breadcrumb className='!relative !z-0 !top-auto !p-0 !border-none' />;
};`;
