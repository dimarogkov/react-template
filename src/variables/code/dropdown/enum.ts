export const DROPDOWN_ENUM_CODE = `export enum EnumDropdownAlign {
    start = 'start',
    end = 'end',
}

export enum EnumDropdownPosition {
    top = 'top',
    left = 'left',
    right = 'right',
    bottom = 'bottom',
}`;
